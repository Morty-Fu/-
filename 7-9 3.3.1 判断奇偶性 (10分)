#include <stdio.h>

int even( int n );

int main()
{    
    int n;

    scanf("%d", &n);
    if (even(n))
        printf("Even\n");
    else
        printf("Odd\n");

    return 0;
}

int even( int n )
{
	return !(n%2); 
}
